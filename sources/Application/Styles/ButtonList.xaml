<UserControl x:Class="Views.ButtonList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Style="{StaticResource btn}" Grid.Row="0">Bandes annonces</Button>
        <Button Style="{StaticResource btn}" Click="Film_Click" Grid.Row="1">Meilleurs films</Button>
        <Button Style="{StaticResource btn}" Click="Categorie_Click" Grid.Row="2">Catégories</Button>
    </Grid>
    
</UserControl>
